{{!-- added name and id of game to display on homepage --}}
{{#each games as |game| }}
<div class="row mb-4 game">
  <div class="col-md-5">
    <h2>
      <a href="/game/{{game.id}}">{{game.name}}</a>
    </h2>
</div>
</div>
{{/each}}