<!-- Loop over each object in the projects array -->
{{#each games as |game| }}
<div class="row mb-4 game">
  <div class="col-md-5">
    <h2>
      <a href="/game/{{game.id}}">{{game.name}}</a>
    </h2>